---
description: Run the 6-role dev-team pipeline on a task description
argument-hint: "<task description>"
---

Invoke the `dev-team` skill to orchestrate the full pipeline (BA → TechLead → Dev → BA Review → Tester → optional Builder) on this task:

$ARGUMENTS
